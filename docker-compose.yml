services:
  resin:
    build:
      context: .
    image: resin-local:dev
    container_name: resin
    restart: unless-stopped
    env_file:
      - .env
    environment:
      RESIN_LISTEN_ADDRESS: 0.0.0.0
    ports:
      - "12260:12260"
    volumes:
      - ./data/cache:/var/cache/resin
      - ./data/state:/var/lib/resin
      - ./data/log:/var/log/resin
